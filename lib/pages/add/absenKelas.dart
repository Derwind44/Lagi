import 'package:flutter/material.dart';

class AbsenKelas extends StatelessWidget {
  const AbsenKelas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Absen Kelas")),
      body: Container(color: Colors.cyan,),
    );
  }
}